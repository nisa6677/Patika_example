const circleArea = (radius) => {
    let alan = Math.PI(radius **2)
    console.log(alan);
}
const circleCircumFerence = (radius) => {
    let circleCircumFerence = Math.round(radius.Math.PI*2)
    console.log(circleCircumFerence);
}
module.exports = {
    circleArea,
    circleCircumFerence
}